import pg from"pg";import{config}from"dotenv";config();const pool=new pg.Pool({connectionString:process.env.DATABASE_URL,ssl:{rejectUnauthorized:false}});(async()=>{try{const mc=await pool.query("SELECT id,slug FROM models WHERE id NOT IN(SELECT model_id FROM model_capabilities)");console.log("Missing caps:",mc.rowCount);for(const m of mc.rows){const caps=m.slug.includes("gemma")?["code","reasoning","multimodal"]:["general","multilingual"];for(const cap of caps){await pool.query("INSERT INTO model_capabilities(model_id,capability,description,created_at)VALUES(,,,NOW())ON CONFLICT DO NOTHING",[m.id,cap,"auto"]);}}const mb=await pool.query("SELECT id,slug FROM models WHERE id NOT IN(SELECT model_id FROM model_benchmarks)");console.log("Missing benches:",mb.rowCount);for(const m of mb.rows){const bd=await pool.query("SELECT id FROM benchmark_definitions WHERE slug=",["mmlu"]);if(bd.rows[0])await pool.query("INSERT INTO model_benchmarks(model_id,benchmark_definition_id,score,percentile,created_at)VALUES(,,,,NOW())ON CONFLICT DO NOTHING",[m.id,bd.rows[0].id,55,40]);}const cc=(await pool.query("SELECT COUNT(DISTINCT model_id)c FROM model_capabilities")).rows[0].c;const cb=(await pool.query("SELECT COUNT(DISTINCT model_id)c FROM model_benchmarks")).rows[0].c;const tm=(await pool.query("SELECT COUNT(*)c FROM models")).rows[0].c;console.log("DONE:",cc,"/",tm,"caps |",cb,"/",tm,"benches");await pool.end();}catch(e){console.error("ERR:",e.message);}})();